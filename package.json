{
  "name": "dispatch-platform",
  "version": "0.1.0-dev.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.23.0 <25"
  },
  "scripts": {
    "dev": "tsx tooling/dev.ts",
    "start": "cd .build && ./services/rust/dispatch-backend serve",
    "build": "tsx tooling/build.ts",
    "check": "tsc --noEmit",
    "check:ci": "tsx tooling/checks.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "tsx --test --test-concurrency=1 tests/*.test.ts",
    "test:browser": "playwright test",
    "test:ui": "tsx tooling/browser-check.ts",
    "test:smoke": "tsx tooling/browser-check.ts --smoke-only",
    "test:native": "DISPATCH_TEST_NATIVE=1 tsx --test --test-concurrency=1 tests/native-browser.test.ts tests/paycom-native.test.ts tests/paycom-worker.test.ts tests/multi-dsp-browser.test.ts",
    "test:browser-host": "tsx tooling/browser-preflight.ts",
    "test:browseros": "python3 tooling/browseros-check.py",
    "test:artifact": "DISPATCH_TEST_ARTIFACT=1 tsx --test tests/artifact.test.ts",
    "verify": "npm run check && npm test && npm run build",
    "dispatch": "tsx tooling/cli.ts",
    "predev": "cargo build --locked",
    "pretest": "cargo build --locked",
    "test:rust": "cargo test --locked",
    "benchmark:rust": "cargo build --release --locked && tsx tooling/benchmark-rust.ts",
    "check:rust": "cargo fmt --check && cargo clippy --locked --all-targets -- -D warnings && cargo test --locked"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "playwright": "^1.55.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.19.0",
    "@types/react": "^19.1.16",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.4",
    "esbuild": "^0.25.11",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^7.1.12"
  }
}
