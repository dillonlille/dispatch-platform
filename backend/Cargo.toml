[package]
name = "dispatch-backend"
version = "0.0.10"
edition = "2024"
rust-version = "1.94"
publish = false

[dependencies]
axum = { version = "0.8", default-features = false, features = ["http1", "json", "tokio"] }
rusqlite = { version = "0.40", features = ["bundled", "functions", "collation"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["fs", "io-std", "io-util", "macros", "net", "process", "rt-multi-thread", "signal", "sync", "time"] }
aes-gcm = "0.10"
argon2 = "0.5"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"
fs2 = "0.4"
getrandom = "0.3"
hmac = "0.12"
libc = "0.2"
icu_collator = "2"
sha2 = "0.10"
subtle = "2"
url = "2"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
lettre = { version = "0.11", default-features = false, features = ["builder", "smtp-transport", "rustls-tls"] }

[dev-dependencies]
tempfile = "3"
# Writes shared/contracts/generated from the response types; never part of the binary.
ts-rs = { version = "12", default-features = false, features = ["serde-compat"] }
