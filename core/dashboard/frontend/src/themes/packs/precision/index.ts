import { defineTheme } from "../../types.ts";

export default defineTheme({
  apiVersion: 1,
  id: "precision",
  name: "Precision",
  description: "Clear typography, quiet surfaces, and a cobalt accent.",
});
