{
  "name": "dispatch-api",
  "version": "0.1.0",
  "private": true,
  "description": "Authenticated Dispatch HTTP API and backend service composition",
  "type": "commonjs",
  "main": "server.js",
  "engines": { "node": ">=22" },
  "scripts": { "start": "../../bin/dispatch-api", "test": "node --no-warnings --test tests/*.test.js" }
}
