#!/usr/bin/env bash
set -euo pipefail
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
"$ROOT/tooling/build"
"$ROOT/tooling/test"
node --no-warnings "$ROOT/examples/two-runtime-agents.js"
node --no-warnings "$ROOT/examples/durable-service.js"
