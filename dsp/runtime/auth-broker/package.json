{
  "name": "dispatch-auth-broker",
  "version": "0.9.0",
  "private": true,
  "description": "Local credential vault and authentication-session broker for Dispatch collectors",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "./scripts/build",
    "verify": "./scripts/verify",
    "health": "./scripts/health"
  }
}
