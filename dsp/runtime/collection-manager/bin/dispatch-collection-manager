#!/usr/bin/env -S node --no-warnings
'use strict';
process.umask(0o077);
process.env.NODE_NO_WARNINGS = '1';
require('../src/daemon-cli').main().then(code => { if (Number.isInteger(code)) process.exitCode = code; });
