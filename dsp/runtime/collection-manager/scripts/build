#!/usr/bin/env bash
set -euo pipefail
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
shopt -s nullglob
files=("$ROOT"/src/*.js "$ROOT"/tests/*.js "$ROOT"/bin/*)
for file in "${files[@]}"; do
  node --check "$file" >/dev/null
done
printf '%s\n' '{"ok":true,"status":"built"}'
