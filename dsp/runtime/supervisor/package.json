{
  "name": "dispatch-runtime-container",
  "version": "0.1.0",
  "private": true,
  "description": "Fixed non-root supervisor and health boundary for one isolated DSP runtime container",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "./scripts/build",
    "test": "./scripts/test",
    "verify": "./scripts/verify"
  }
}
