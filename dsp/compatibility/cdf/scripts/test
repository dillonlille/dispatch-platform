#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
rm -rf "$ROOT/tests/.fixture-runtime"
trap 'rm -rf "$ROOT/tests/.fixture-runtime"' EXIT
node --no-warnings --test "$ROOT"/tests/*.test.js
