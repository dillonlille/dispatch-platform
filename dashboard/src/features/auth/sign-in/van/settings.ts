/** Presentation knobs; colors and contour styling live in auth.css. */
export const vanSettings = {
  rotationSeconds: 24,
  framesPerSecond: 30,
  maxPixelRatio: 1.5,
  maxPixels: 1_500_000,
  textureScale: 1.7,
  orderedPixelSize: 1.65,
  cameraPosition: [-9, 6, 14] as const,
  cameraTarget: [0, 1.4, 0] as const,
  horizontalFraming: 5.15,
  minimumVerticalFraming: 2.55,
};
